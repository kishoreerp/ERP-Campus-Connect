# ERP Campus Connect

College ERP app with Flutter frontend and Node.js backend.

## 📁 Folder Structure
- `frontend/` → Flutter app (UI)
- `backend/` → Node.js server (API)
- `.gitignore` → Common ignore rules

## 🚀 Setup Instructions

### Frontend
```bash
cd frontend
flutter pub get
flutter run

### Backend
```bash
cd backend
npm install
npm start
